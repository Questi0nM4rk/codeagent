# =============================================================================
# STRICT Pre-Commit Configuration for AI-Driven Development
# =============================================================================
# CodeAgent framework - Multi-language hooks (Shell, Python, Markdown, YAML)
# All checks run in strictest mode. No exceptions. No bypasses.
# AI agents need guardrails - this is the fence.
# =============================================================================

fail_fast: true
default_language_version:
  python: python3.12

repos:
  # ===========================================================================
  # 🔒 SECURITY - CRITICAL (runs first, fails fast)
  # ===========================================================================

  # Detect hardcoded secrets, API keys, passwords
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks
        name: "🔒 security · gitleaks - detect secrets"

  # Detect private keys
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: detect-private-key
        name: "🔒 security · detect private keys"

  # ===========================================================================
  # 🐚 SHELL - STRICT MODE
  # ===========================================================================

  - repo: https://github.com/shellcheck-py/shellcheck-py
    rev: v0.10.0.1
    hooks:
      - id: shellcheck
        name: "🐚 shell · shellcheck lint (strict)"
        args: ["--severity=warning", "--shell=bash"]
        types: [shell]

  # Shell formatting
  - repo: https://github.com/scop/pre-commit-shfmt
    rev: v3.10.0-1
    hooks:
      - id: shfmt
        name: "🐚 shell · shfmt format check"
        args: ["-i", "2", "-ci", "-bn", "-d"]  # 2-space indent, case indent, binary ops newline, diff

  # ===========================================================================
  # 🐍 PYTHON - STRICT MODE (for any Python in the project)
  # ===========================================================================

  # Ruff - Fast linter
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.6
    hooks:
      - id: ruff
        name: "🐍 python · ruff lint (strict)"
        args: [
          "--fix",
          "--exit-non-zero-on-fix",
          "--select=ALL",
          "--ignore=D,ANN,COM812,ISC001",
        ]
      - id: ruff-format
        name: "🐍 python · ruff format"

  # Bandit - Security linter for Python
  - repo: https://github.com/PyCQA/bandit
    rev: 1.8.0
    hooks:
      - id: bandit
        name: "🐍 python · bandit security scan"
        args: ["-ll", "-ii"]
        exclude: ^tests/

  # ===========================================================================
  # 📝 MARKDOWN - STRICT MODE
  # ===========================================================================

  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.43.0
    hooks:
      - id: markdownlint-cli2
        name: "📝 markdown · markdownlint"

  # ===========================================================================
  # 📋 CONFIG & DATA VALIDATION
  # ===========================================================================

  - repo: https://github.com/python-jsonschema/check-jsonschema
    rev: 0.30.0
    hooks:
      - id: check-github-workflows
        name: "🐙 github · validate workflow files"
        args: ["--verbose"]
      - id: check-github-actions
        name: "🐙 github · validate action files"

  # ===========================================================================
  # 🔧 UNIVERSAL QUALITY CHECKS
  # ===========================================================================

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      # Whitespace & formatting
      - id: trailing-whitespace
        name: "🔧 format · trailing whitespace"
        args: [--markdown-linebreak-ext=md]
      - id: end-of-file-fixer
        name: "🔧 format · end of file fixer"
      - id: mixed-line-ending
        name: "🔧 format · mixed line endings"
        args: [--fix=lf]

      # Syntax validation
      - id: check-yaml
        name: "🔧 syntax · check yaml"
        args: [--unsafe]
      - id: check-json
        name: "🔧 syntax · check json"
      - id: check-toml
        name: "🔧 syntax · check toml"

      # Git hygiene
      - id: check-merge-conflict
        name: "🔧 git · check merge conflicts"
      - id: check-added-large-files
        name: "🔧 git · check large files"
        args: [--maxkb=500]
      - id: no-commit-to-branch
        name: "🔧 git · no commit to main"
        args: [--branch=main, --branch=master]

      # File checks
      - id: check-case-conflict
        name: "🔧 files · case conflict"
      - id: check-symlinks
        name: "🔧 files · broken symlinks"
      - id: check-executables-have-shebangs
        name: "🔧 files · executables have shebangs"
      - id: check-shebang-scripts-are-executable
        name: "🔧 files · shebang scripts executable"

# =============================================================================
# CI Note: This config is also used in GitHub Actions
# Install: pip install pre-commit && pre-commit install
# Run all: pre-commit run --all-files
# =============================================================================
